<?xml version="1.0" encoding="utf-8"?>
<templates xml:space="preserve">

<!-- ── Root Dashboard ─────────────────────────────────────────────────────── -->
<t t-name="aura_backend_theme.HomeDashboard">
    <div t-att-class="dashboardRootClass">

        <t t-if="state.loading">
            <div class="hd-skeleton">
                <!-- Topbar -->
                <div class="hd-skeleton-topbar">
                    <div class="hd-skeleton-topbar-left">
                        <div class="hd-skel hd-skel-avatar"/>
                        <div class="hd-skeleton-greeting">
                            <div class="hd-skel hd-skel-line" style="width:160px"/>
                            <div class="hd-skel hd-skel-line hd-skel-line--sm" style="width:100px"/>
                        </div>
                    </div>
                    <div class="hd-skel hd-skel-btn"/>
                </div>

                <!-- Body -->
                <div class="hd-skeleton-body">

                    <!-- Stats row: 4 cards -->
                    <div class="hd-skeleton-stats">
                        <div class="hd-skeleton-stat-card">
                            <div class="hd-skeleton-stat-top">
                                <div class="hd-skel hd-skel-icon"/>
                                <div class="hd-skel hd-skel-badge"/>
                            </div>
                            <div class="hd-skel hd-skel-value"/>
                            <div class="hd-skel hd-skel-line hd-skel-line--sm"/>
                            <div class="hd-skel hd-skel-stat-action"/>
                        </div>
                        <div class="hd-skeleton-stat-card">
                            <div class="hd-skeleton-stat-top">
                                <div class="hd-skel hd-skel-icon"/>
                                <div class="hd-skel hd-skel-badge"/>
                            </div>
                            <div class="hd-skel hd-skel-value"/>
                            <div class="hd-skel hd-skel-line hd-skel-line--sm"/>
                            <div class="hd-skel hd-skel-stat-action"/>
                        </div>
                        <div class="hd-skeleton-stat-card">
                            <div class="hd-skeleton-stat-top">
                                <div class="hd-skel hd-skel-icon"/>
                                <div class="hd-skel hd-skel-badge"/>
                            </div>
                            <div class="hd-skel hd-skel-value"/>
                            <div class="hd-skel hd-skel-line hd-skel-line--sm"/>
                            <div class="hd-skel hd-skel-stat-action"/>
                        </div>
                        <div class="hd-skeleton-stat-card">
                            <div class="hd-skeleton-stat-top">
                                <div class="hd-skel hd-skel-icon"/>
                            </div>
                            <div class="hd-skel hd-skel-value"/>
                            <div class="hd-skel hd-skel-line hd-skel-line--sm"/>
                            <div class="hd-skel hd-skel-stat-action"/>
                        </div>
                    </div>

                    <!-- Widget grid -->
                    <div class="hd-skeleton-grid">

                        <!-- Wide card (col-2) -->
                        <div class="hd-skeleton-widget-card hd-skeleton-col-2">
                            <div class="hd-skeleton-card-header">
                                <div class="hd-skel hd-skel-title"/>
                                <div class="hd-skel hd-skel-link"/>
                            </div>
                            <div class="hd-skeleton-row">
                                <div class="hd-skel hd-skel-row-icon"/>
                                <div class="hd-skeleton-row-body">
                                    <div class="hd-skel hd-skel-line"/>
                                    <div class="hd-skel hd-skel-line hd-skel-line--sm"/>
                                </div>
                                <div class="hd-skeleton-row-right">
                                    <div class="hd-skel hd-skel-amount"/>
                                    <div class="hd-skel hd-skel-badge"/>
                                </div>
                            </div>
                            <div class="hd-skeleton-row">
                                <div class="hd-skel hd-skel-row-icon"/>
                                <div class="hd-skeleton-row-body">
                                    <div class="hd-skel hd-skel-line"/>
                                    <div class="hd-skel hd-skel-line hd-skel-line--sm"/>
                                </div>
                                <div class="hd-skeleton-row-right">
                                    <div class="hd-skel hd-skel-amount"/>
                                    <div class="hd-skel hd-skel-badge"/>
                                </div>
                            </div>
                            <div class="hd-skeleton-row">
                                <div class="hd-skel hd-skel-row-icon"/>
                                <div class="hd-skeleton-row-body">
                                    <div class="hd-skel hd-skel-line"/>
                                    <div class="hd-skel hd-skel-line hd-skel-line--sm"/>
                                </div>
                                <div class="hd-skeleton-row-right">
                                    <div class="hd-skel hd-skel-amount"/>
                                    <div class="hd-skel hd-skel-badge"/>
                                </div>
                            </div>
                            <div class="hd-skeleton-row">
                                <div class="hd-skel hd-skel-row-icon"/>
                                <div class="hd-skeleton-row-body">
                                    <div class="hd-skel hd-skel-line"/>
                                    <div class="hd-skel hd-skel-line hd-skel-line--sm"/>
                                </div>
                                <div class="hd-skeleton-row-right">
                                    <div class="hd-skel hd-skel-amount"/>
                                    <div class="hd-skel hd-skel-badge"/>
                                </div>
                            </div>
                            <div class="hd-skeleton-row">
                                <div class="hd-skel hd-skel-row-icon"/>
                                <div class="hd-skeleton-row-body">
                                    <div class="hd-skel hd-skel-line"/>
                                    <div class="hd-skel hd-skel-line hd-skel-line--sm"/>
                                </div>
                                <div class="hd-skeleton-row-right">
                                    <div class="hd-skel hd-skel-amount"/>
                                    <div class="hd-skel hd-skel-badge"/>
                                </div>
                            </div>
                        </div>

                        <!-- Narrow card (col-1) -->
                        <div class="hd-skeleton-widget-card hd-skeleton-col-1">
                            <div class="hd-skeleton-card-header">
                                <div class="hd-skel hd-skel-title"/>
                                <div class="hd-skel hd-skel-link"/>
                            </div>
                            <div class="hd-skeleton-row">
                                <div class="hd-skel hd-skel-row-icon"/>
                                <div class="hd-skeleton-row-body">
                                    <div class="hd-skel hd-skel-line"/>
                                    <div class="hd-skel hd-skel-line hd-skel-line--sm"/>
                                </div>
                                <div class="hd-skel hd-skel-badge"/>
                            </div>
                            <div class="hd-skeleton-row">
                                <div class="hd-skel hd-skel-row-icon"/>
                                <div class="hd-skeleton-row-body">
                                    <div class="hd-skel hd-skel-line"/>
                                    <div class="hd-skel hd-skel-line hd-skel-line--sm"/>
                                </div>
                                <div class="hd-skel hd-skel-badge"/>
                            </div>
                            <div class="hd-skeleton-row">
                                <div class="hd-skel hd-skel-row-icon"/>
                                <div class="hd-skeleton-row-body">
                                    <div class="hd-skel hd-skel-line"/>
                                    <div class="hd-skel hd-skel-line hd-skel-line--sm"/>
                                </div>
                                <div class="hd-skel hd-skel-badge"/>
                            </div>
                            <div class="hd-skeleton-row">
                                <div class="hd-skel hd-skel-row-icon"/>
                                <div class="hd-skeleton-row-body">
                                    <div class="hd-skel hd-skel-line"/>
                                    <div class="hd-skel hd-skel-line hd-skel-line--sm"/>
                                </div>
                                <div class="hd-skel hd-skel-badge"/>
                            </div>
                        </div>

                        <!-- Three narrow cards (col-1 each) -->
                        <div class="hd-skeleton-widget-card hd-skeleton-col-1">
                            <div class="hd-skeleton-card-header">
                                <div class="hd-skel hd-skel-title"/>
                                <div class="hd-skel hd-skel-link"/>
                            </div>
                            <div class="hd-skeleton-row">
                                <div class="hd-skel hd-skel-row-icon"/>
                                <div class="hd-skeleton-row-body">
                                    <div class="hd-skel hd-skel-line"/>
                                    <div class="hd-skel hd-skel-line hd-skel-line--sm"/>
                                </div>
                                <div class="hd-skel hd-skel-badge"/>
                            </div>
                            <div class="hd-skeleton-row">
                                <div class="hd-skel hd-skel-row-icon"/>
                                <div class="hd-skeleton-row-body">
                                    <div class="hd-skel hd-skel-line"/>
                                    <div class="hd-skel hd-skel-line hd-skel-line--sm"/>
                                </div>
                                <div class="hd-skel hd-skel-badge"/>
                            </div>
                            <div class="hd-skeleton-row">
                                <div class="hd-skel hd-skel-row-icon"/>
                                <div class="hd-skeleton-row-body">
                                    <div class="hd-skel hd-skel-line"/>
                                    <div class="hd-skel hd-skel-line hd-skel-line--sm"/>
                                </div>
                                <div class="hd-skel hd-skel-badge"/>
                            </div>
                        </div>
                        <div class="hd-skeleton-widget-card hd-skeleton-col-1">
                            <div class="hd-skeleton-card-header">
                                <div class="hd-skel hd-skel-title hd-skel-title--wide"/>
                                <div class="hd-skel hd-skel-link"/>
                            </div>
                            <div class="hd-skeleton-row">
                                <div class="hd-skel hd-skel-row-icon"/>
                                <div class="hd-skeleton-row-body">
                                    <div class="hd-skel hd-skel-line"/>
                                    <div class="hd-skel hd-skel-line hd-skel-line--sm"/>
                                </div>
                                <div class="hd-skel hd-skel-badge"/>
                            </div>
                            <div class="hd-skeleton-row">
                                <div class="hd-skel hd-skel-row-icon"/>
                                <div class="hd-skeleton-row-body">
                                    <div class="hd-skel hd-skel-line"/>
                                    <div class="hd-skel hd-skel-line hd-skel-line--sm"/>
                                </div>
                                <div class="hd-skel hd-skel-badge"/>
                            </div>
                            <div class="hd-skeleton-row">
                                <div class="hd-skel hd-skel-row-icon"/>
                                <div class="hd-skeleton-row-body">
                                    <div class="hd-skel hd-skel-line"/>
                                    <div class="hd-skel hd-skel-line hd-skel-line--sm"/>
                                </div>
                                <div class="hd-skel hd-skel-badge"/>
                            </div>
                        </div>
                        <div class="hd-skeleton-widget-card hd-skeleton-col-1">
                            <div class="hd-skeleton-card-header">
                                <div class="hd-skel hd-skel-title"/>
                                <div class="hd-skel hd-skel-link"/>
                            </div>
                            <div class="hd-skeleton-row">
                                <div class="hd-skel hd-skel-row-icon"/>
                                <div class="hd-skeleton-row-body">
                                    <div class="hd-skel hd-skel-line"/>
                                    <div class="hd-skel hd-skel-line hd-skel-line--sm"/>
                                </div>
                                <div class="hd-skel hd-skel-badge"/>
                            </div>
                            <div class="hd-skeleton-row">
                                <div class="hd-skel hd-skel-row-icon"/>
                                <div class="hd-skeleton-row-body">
                                    <div class="hd-skel hd-skel-line"/>
                                    <div class="hd-skel hd-skel-line hd-skel-line--sm"/>
                                </div>
                                <div class="hd-skel hd-skel-badge"/>
                            </div>
                            <div class="hd-skeleton-row">
                                <div class="hd-skel hd-skel-row-icon"/>
                                <div class="hd-skeleton-row-body">
                                    <div class="hd-skel hd-skel-line"/>
                                    <div class="hd-skel hd-skel-line hd-skel-line--sm"/>
                                </div>
                                <div class="hd-skel hd-skel-badge"/>
                            </div>
                        </div>

                    </div>
                </div>
            </div>
        </t>

        <t t-elif="state.error">
            <div class="hd-error">
                <i class="fa fa-exclamation-triangle"/>
                <span t-esc="state.error"/>
            </div>
        </t>

        <t t-elif="state.data">
            <DashboardTopBar
                user="state.data.user"
                onOpenConfig="() => this.openSettingsFromMenu()"
                onOpenPicker="() => this.openWidgetsFromMenu()"
                onOpenCustomWidget="() => this.openCustomWidgetFromMenu()"
                onToggleEdit="() => this.openEditModeFromMenu()"
                onToggleMenu="() => this.toggleTopbarMenu()"
                editMode="state.editMode"
                menuOpen="state.topbarMenuOpen"/>

            <!-- Insight banners (full-width, above body) -->
            <t t-if="state.data.insights and state.data.insights.length">
                <InsightBanners insights="state.data.insights"/>
            </t>

            <!-- Dynamic widget grid -->
            <div class="hd-widget-grid hd-body" t-ref="widgetGrid">
                <t t-foreach="renderedWidgets" t-as="w" t-key="w.widget_id">
                    <div
                        t-attf-class="hd-widget-cell hd-col-#{w.col_span} {{ state.dragging === w.widget_id ? 'hd-dragging' : '' }} {{ state.dragOver === w.widget_id ? 'hd-drag-over' : '' }}"
                        t-att-draggable="state.editMode ? 'true' : 'false'"
                        t-on-dragstart="() => this.onDragStart(w.widget_id)"
                        t-on-dragover.prevent="() => this.onDragOver(w.widget_id)"
                        t-on-dragend="() => this.onDragEnd()">

                        <!-- Resize handle -->
                        <div t-if="state.editMode" class="hd-widget-controls">
                            <button t-if="!w.missing and w.col_span !== '1'" class="hd-resize-btn" title="Shrink"
                                    t-on-click.stop="() => this.shrinkWidget(w.widget_id, w.col_span)">
                                <i class="fa fa-compress-alt"/>
                            </button>
                            <button t-if="!w.missing and w.col_span !== '3'" class="hd-resize-btn" title="Expand"
                                    t-on-click.stop="() => this.expandWidget(w.widget_id, w.col_span)">
                                <i class="fa fa-expand-alt"/>
                            </button>
                            <button t-if="!w.missing" class="hd-resize-btn" title="Widget settings"
                                    t-on-click.stop="() => this.openWidgetSettings(w.widget)">
                                <i class="fa fa-cog"/>
                            </button>
                            <button class="hd-remove-btn" title="Remove widget"
                                    t-on-click.stop="() => this.removeWidget(w.widget_id)">
                                <i class="fa fa-times"/>
                            </button>
                        </div>

                        <t t-if="w.missing">
                            <WidgetUnavailable widget="w.widget" onRemove="() => this.removeWidget(w.widget_id)"/>
                        </t>
                        <t t-else="">
                            <t t-component="w.component" t-props="w.props" t-key="w.widget_id"/>
                        </t>
                    </div>
                </t>

                <!-- Add widget cell -->
                <div t-if="state.editMode or !renderedWidgets.length" class="hd-widget-add-cell hd-col-1">
                    <button t-if="state.editMode" class="hd-add-widget-btn" t-on-click="() => this.togglePicker()">
                        <i class="fa fa-plus-circle"/>
                        <span t-esc="renderedWidgets.length ? 'Add Widget' : 'No widgets yet — Add your first widget'"/>
                    </button>
                    <div t-else="" class="hd-add-widget-hint">
                        <i class="fa fa-lock"/>
                        <span t-esc="renderedWidgets.length ? 'Enable edit mode to add widgets' : 'No widgets yet. Enable edit mode, then click Add Widget'"/>
                    </div>
                </div>
            </div>

            <!-- Config drawer -->
            <DashboardConfigPanel
                config="state.data.config"
                stats="state.data.stats"
                open="state.configOpen"
                onClose="() => this.toggleConfig()"
                onSave="(v) => this.saveConfig(v)"/>

            <t t-if="state.topbarMenuOpen">
                <div class="hd-gear-overlay" t-on-click="() => this.closeTopbarMenu()"/>
            </t>

            <!-- Widget picker -->
            <WidgetPicker
                open="state.pickerOpen"
                widgets="state.availableWidgets"
                onClose="() => this.togglePicker()"
                onAdd="(id) => this.addWidget(id)"
                onRemove="(id) => this.removeWidget(id)"
                onRestoreDefaults="() => this.restoreDefaultWidgets()"/>

            <WidgetSettingsModal
                open="state.settingsOpen"
                widget="state.settingsWidget"
                draft="state.settingsDraft"
                error="state.settingsError"
                onClose="() => this.closeWidgetSettings()"
                onSave="() => this.saveWidgetSettings()"
                onDraftChange="(value) => this.updateWidgetSettingsDraft(value)"/>

            <CustomWidgetBuilder
                open="state.customBuilderOpen"
                models="state.customModels"
                fields="state.customFields"
                draft="state.customDraft"
                error="state.customError"
                loading="state.customLoading"
                onClose="() => this.closeCustomWidgetBuilder()"
                onSave="() => this.saveCustomWidget()"
                onDraftChange="(values) => this.updateCustomDraft(values)"
                onModelChange="(model) => this.onCustomModelChange(model)"/>
        </t>
    </div>
</t>

<!-- ── Top Bar ────────────────────────────────────────────────────────────── -->
<t t-name="aura_backend_theme.DashboardTopBar">
    <div class="hd-topbar">
        <div class="hd-topbar-left">
            <img class="hd-avatar" t-att-src="props.user.avatar_url" alt=""/>
            <div class="hd-greeting">
                <div class="hd-topbar-badge">Tableau de bord personnel</div>
                <div class="hd-greeting-line">
                    <span class="hd-greeting-prefix"><span t-esc="greeting()"/>, </span><strong t-esc="props.user.name"/>
                </div>
                <div class="hd-company" t-esc="props.user.company_name"/>
            </div>
        </div>
        <div class="hd-topbar-right">
            <div t-attf-class="hd-gear-menu {{ props.menuOpen ? 'open' : '' }}">
                <button class="hd-gear-btn" t-on-click="props.onToggleMenu" title="Dashboard actions">
                    <i class="fa fa-cog"/>
                </button>
                <div t-if="props.menuOpen" class="hd-gear-menu-panel">
                    <button t-attf-class="hd-gear-item {{ props.editMode ? 'is-active' : '' }}" t-on-click="props.onToggleEdit">
                        <i t-attf-class="fa {{ props.editMode ? 'fa-check-circle' : 'fa-pen' }}"/>
                        <span t-esc="props.editMode ? 'Editing' : 'Edit dashboard'"/>
                    </button>
                    <button class="hd-gear-item" t-on-click="props.onOpenPicker">
                        <i class="fa fa-th-large"/>
                        <span>Widgets</span>
                    </button>
                    <button class="hd-gear-item" t-on-click="props.onOpenCustomWidget">
                        <i class="fa fa-magic"/>
                        <span>Widget personnalisé</span>
                    </button>
                    <button class="hd-gear-item" t-on-click="props.onOpenConfig">
                        <i class="fa fa-sliders-h"/>
                        <span>Settings</span>
                    </button>
                </div>
            </div>
        </div>
    </div>
</t>

<!-- ── Quick Actions Bar ──────────────────────────────────────────────────── -->
<t t-name="aura_backend_theme.QuickActionsBar">
    <div class="hd-quick-actions">
        <t t-foreach="props.actions" t-as="action" t-key="action.url">
            <button
                t-attf-class="hd-quick-btn hd-quick-btn--#{action.color}"
                t-on-click="() => this.navigate(action.action or action.url)">
                <i t-attf-class="fa #{action.icon}"/>
                <span t-esc="action.label"/>
            </button>
        </t>
    </div>
</t>

<!-- ── Insight Banners ────────────────────────────────────────────────────── -->
<t t-name="aura_backend_theme.InsightBanners">
    <div class="hd-insights">
        <t t-foreach="props.insights" t-as="insight" t-key="insight.type + '_' + insight.message">
            <div t-attf-class="hd-insight hd-insight--#{insight.type}">
                <i t-attf-class="fa #{insight.icon}"/>
                <span t-esc="insight.message"/>
                <button class="hd-insight-action" t-on-click="() => this.navigate(insight.action or insight.action_url)">
                    <t t-esc="insight.action_label"/> →
                </button>
            </div>
        </t>
    </div>
</t>

<!-- ── Stat Card ──────────────────────────────────────────────────────────── -->
<t t-name="aura_backend_theme.StatCard">
    <div t-attf-class="hd-stat-card hd-stat-#{props.color}">
        <div class="hd-stat-top">
            <div class="hd-stat-icon">
                <i t-attf-class="fa #{props.icon}"/>
            </div>
            <t t-if="props.trend != null">
                <span t-attf-class="hd-trend #{trendClass}">
                    <i t-attf-class="fa #{trendIcon}"/>
                    <t t-esc="trendLabel"/>
                </span>
            </t>
        </div>
        <div class="hd-stat-value" t-esc="displayValue"/>
        <div class="hd-stat-label" t-esc="props.label"/>
        <div t-if="props.meta" class="hd-stat-meta" t-esc="props.meta"/>
        <div class="hd-stat-actions">
            <button t-if="props.viewAction or props.viewUrl" class="hd-stat-link" t-on-click="() => this.navigate(props.viewAction or props.viewUrl)">Voir →</button>
            <button t-if="props.createAction or props.createUrl" class="hd-stat-link hd-stat-create" t-on-click="() => this.navigate(props.createAction or props.createUrl)">+ Nouveau</button>
        </div>
    </div>
</t>

<!-- ── Stats Row ──────────────────────────────────────────────────────────── -->
<t t-name="aura_backend_theme.DashboardStatsRow">
    <div class="hd-stats-row">
        <t t-foreach="enabledStats" t-as="stat" t-key="stat.key">
            <StatCard
                label="stat.label" value="stat.value" meta="stat.meta"
                color="stat.color" icon="stat.icon"
                viewUrl="stat.viewUrl" createUrl="stat.createUrl" viewAction="stat.viewAction" createAction="stat.createAction" trend="stat.trend"
                monetary="stat.monetary" currencySymbol="stat.currencySymbol" currencyPosition="stat.currencyPosition"/>
        </t>
    </div>
</t>

<!-- ── Activities Widget ──────────────────────────────────────────────────── -->
<t t-name="aura_backend_theme.WidgetActivities">
    <div class="hd-card hd-widget-activities">
        <div class="hd-card-header">
            <span class="hd-card-title"><i class="fa fa-tasks"/> Mes activités</span>
            <button class="hd-card-link hd-link-btn" t-on-click="() => this.navigate('/odoo/todos')">Tout voir →</button>
        </div>
        <div class="hd-card-body">
            <t t-if="!props.activities.length">
                <div class="hd-empty-cta">
                    <i class="fa fa-check-circle hd-empty-icon"/>
                    <p>Tout est à jour ! Aucune activité en attente.</p>
                    <button class="hd-cta-btn" t-on-click="() => this.navigate('/odoo/todos')">Planifier une activité</button>
                </div>
            </t>
            <t t-foreach="groups" t-as="group" t-key="group.key">
                <div t-attf-class="hd-activity-group {{ group.urgent ? 'hd-group-urgent' : '' }}">
                    <div class="hd-group-header">
                        <span class="hd-group-label" t-esc="group.label"/>
                        <span class="hd-group-count" t-esc="group.items.length"/>
                    </div>
                    <t t-foreach="group.items" t-as="act" t-key="act.id">
                        <div class="hd-activity-row">
                            <div class="hd-activity-icon">
                                <i t-attf-class="fa #{act.activity_type_icon}"/>
                            </div>
                            <div class="hd-activity-body">
                                <button class="hd-activity-name hd-link-btn" t-on-click="() => this.navigate(act.action or act.record_url)" t-esc="act.res_name"/>
                                <div class="hd-activity-type" t-esc="act.summary or act.activity_type_name"/>
                            </div>
                            <div class="hd-activity-right">
                                <span t-attf-class="#{badgeClass(act.days_overdue)}" t-esc="daysLabel(act.days_overdue)"/>
                                <button class="hd-btn-done" t-on-click="() => props.onDone(act.id)" title="Mark done">
                                    <i class="fa fa-check"/>
                                </button>
                            </div>
                        </div>
                    </t>
                </div>
            </t>
        </div>
    </div>
</t>

<!-- ── Invoices Widget ────────────────────────────────────────────────────── -->
<t t-name="aura_backend_theme.WidgetInvoices">
    <div class="hd-card hd-widget-invoices">
        <div class="hd-card-header">
            <span class="hd-card-title"><i class="fa fa-file-invoice-dollar"/> Top Invoices</span>
            <div class="hd-invoice-filters">
                <button t-attf-class="hd-filter-btn #{props.config.invoice_filter === 'all' ? 'active' : ''}"
                        t-on-click="() => props.onFilterChange('all')">Tous</button>
                <button t-attf-class="hd-filter-btn #{props.config.invoice_filter === 'overdue' ? 'active' : ''}"
                        t-on-click="() => props.onFilterChange('overdue')">En retard</button>
                <button t-attf-class="hd-filter-btn #{props.config.invoice_filter === 'this_month' ? 'active' : ''}"
                        t-on-click="() => props.onFilterChange('this_month')">Ce mois-ci</button>
            </div>
        </div>
        <div class="hd-card-body">
            <t t-if="!props.invoices.length">
                <div class="hd-empty-cta">
                    <i class="fa fa-file-invoice-dollar hd-empty-icon"/>
                    <p>Aucune facture pour ce filtre.</p>
                    <button class="hd-cta-btn" t-on-click="() => this.navigate('/odoo/accounting/customer-invoices/new')">
                        Create Invoice
                    </button>
                </div>
            </t>
            <t t-foreach="props.invoices" t-as="inv" t-key="inv.id">
                <div class="hd-invoice-row" t-on-click="() => this.navigate(inv.action or inv.record_url)">
                    <div class="hd-invoice-info">
                        <span class="hd-invoice-name" t-esc="inv.name"/>
                        <span class="hd-invoice-partner" t-esc="inv.partner_name"/>
                    </div>
                    <div class="hd-invoice-right">
                        <span class="hd-invoice-amount">
                            <t t-esc="inv.currency_symbol"/><t t-esc="inv.amount_total.toFixed(2)"/>
                        </span>
                        <span t-attf-class="#{badgeClass(inv.status_label)}" t-esc="inv.status_label"/>
                    </div>
                </div>
            </t>
        </div>
        <div class="hd-card-footer">
            <button class="hd-footer-link hd-link-btn" t-on-click="() => this.navigate('/odoo/accounting/customer-invoices')">Voir toutes les factures →</button>
        </div>
    </div>
</t>

<!-- ── Pipeline Widget ────────────────────────────────────────────────────── -->
<t t-name="aura_backend_theme.WidgetPipeline">
    <div class="hd-card hd-widget-pipeline">
        <div class="hd-card-header">
            <span class="hd-card-title"><i class="fa fa-chart-bar"/> Mon pipeline</span>
            <button class="hd-card-action-btn" t-on-click="() => this.navigate('/odoo/crm/new')">
                <i class="fa fa-plus"/> Nouvelle piste
            </button>
        </div>
        <t t-if="!props.pipeline.length">
            <div class="hd-empty-cta">
                <i class="fa fa-bullseye hd-empty-icon"/>
                <p>Aucune opportunité active dans votre pipeline.</p>
                <button class="hd-cta-btn" t-on-click="() => this.navigate('/odoo/crm/new')">Ajouter une première piste</button>
            </div>
        </t>
        <div class="hd-pipeline-cols">
            <t t-foreach="props.pipeline" t-as="stage" t-key="stage.stage_id">
                <div class="hd-pipeline-col">
                    <div class="hd-pipeline-stage-header">
                        <span class="hd-pipeline-stage-name" t-esc="stage.stage_name"/>
                        <span class="hd-pipeline-count" t-esc="stage.count"/>
                    </div>
                    <div class="hd-pipeline-value">
                        <t t-esc="stage.total_value.toLocaleString(undefined, {minimumFractionDigits:0, maximumFractionDigits:0})"/>
                    </div>
                    <t t-foreach="stage.leads" t-as="lead" t-key="lead.id">
                        <div t-attf-class="hd-opp priority-#{lead.priority}" t-on-click="() => this.navigate(lead.action or lead.record_url)">
                            <div class="hd-opp-name" t-esc="lead.name"/>
                            <div class="hd-opp-partner" t-esc="lead.partner_name"/>
                            <div class="hd-opp-footer">
                                <span class="hd-opp-revenue">
                                    <t t-esc="lead.currency_symbol"/>
                                    <t t-esc="lead.expected_revenue.toLocaleString(undefined, {minimumFractionDigits:0, maximumFractionDigits:0})"/>
                                </span>
                                <t t-if="lead.days_in_stage > 0">
                                    <span t-attf-class="#{staleClass(lead.days_in_stage)}">
                                        <t t-esc="lead.days_in_stage"/>d
                                    </span>
                                </t>
                            </div>
                        </div>
                    </t>
                </div>
            </t>
        </div>
        <div class="hd-card-footer">
            <button class="hd-footer-link hd-link-btn" t-on-click="() => this.navigate('/odoo/crm')">Voir tout le pipeline →</button>
        </div>
    </div>
</t>

<!-- ── Recent Widget ──────────────────────────────────────────────────────── -->
<t t-name="aura_backend_theme.WidgetRecent">
    <div class="hd-card hd-widget-recent">
        <div class="hd-card-header">
            <span class="hd-card-title"><i class="fa fa-history"/> Reprendre le travail</span>
        </div>
        <div class="hd-card-body">
            <t t-if="!props.recent.length">
                <div class="hd-empty">Aucun enregistrement récent</div>
            </t>
            <t t-foreach="props.recent" t-as="rec" t-key="rec.model + rec.res_id">
                <button class="hd-recent-row hd-row-btn" t-on-click="() => this.navigate(rec.action or rec.record_url)">
                    <div class="hd-recent-icon"><i class="fa fa-file-alt"/></div>
                    <div class="hd-recent-body">
                        <span class="hd-recent-name" t-esc="rec.display_name"/>
                        <span class="hd-recent-model" t-esc="rec.model_description"/>
                    </div>
                    <div class="hd-recent-meta">
                        <span class="hd-recent-time" t-esc="timeAgo(rec.last_opened)"/>
                        <span class="hd-recent-resume">Reprendre →</span>
                    </div>
                </button>
            </t>
        </div>
    </div>
</t>

<!-- ── Today's Focus ──────────────────────────────────────────────────────── -->
<t t-name="aura_backend_theme.TodaysFocus">
    <div class="hd-card hd-widget-focus">
        <div class="hd-card-header">
            <span class="hd-card-title"><i class="fa fa-fire"/> Priorités du jour</span>
            <button class="hd-card-link hd-link-btn" t-on-click="() => this.navigate('/odoo/todos')">Tout →</button>
        </div>
        <div class="hd-card-body">
            <t t-if="!hasAnything">
                <div class="hd-empty-cta">
                    <i class="fa fa-sun hd-empty-icon hd-icon-gold"/>
                    <p>Rien d'urgent aujourd'hui. Beau travail !</p>
                </div>
            </t>
            <t t-if="props.insights and props.insights.length">
                <t t-foreach="props.insights" t-as="ins" t-key="ins.type + '_' + ins.message">
                    <div t-attf-class="hd-focus-alert hd-focus-alert--#{ins.type}" t-on-click="() => this.navigate(ins.action or ins.action_url)">
                        <i t-attf-class="fa #{ins.icon}"/>
                        <span t-esc="ins.message"/>
                    </div>
                </t>
            </t>
            <t t-foreach="urgentItems" t-as="act" t-key="act.id">
                <div class="hd-focus-item">
                    <div class="hd-focus-icon"><i t-attf-class="fa #{act.activity_type_icon}"/></div>
                    <div class="hd-focus-body">
                        <button class="hd-focus-record hd-link-btn" t-on-click="() => this.navigate(act.action or act.record_url)" t-esc="act.res_name"/>
                        <span class="hd-focus-type" t-esc="act.summary or act.activity_type_name"/>
                    </div>
                    <div class="hd-focus-right">
                        <span t-attf-class="#{badgeClass(act.days_overdue)}">
                            <t t-if="act.days_overdue > 0"><t t-esc="act.days_overdue"/>d</t>
                            <t t-else="">Aujourd'hui</t>
                        </span>
                        <button class="hd-btn-done" t-on-click="() => props.onDone(act.id)" title="Done">
                            <i class="fa fa-check"/>
                        </button>
                    </div>
                </div>
            </t>
        </div>
    </div>
</t>

<!-- ── Widget Picker ──────────────────────────────────────────────────────── -->
<t t-name="aura_backend_theme.WidgetPicker">
    <t t-if="props.open">
        <div class="hd-picker-overlay" t-on-click.self="props.onClose">
            <div class="hd-picker-modal">
                <div class="hd-picker-header">
                    <div class="hd-picker-title">
                        <i class="fa fa-th-large"/>
                        Add Widgets to Your Dashboard
                    </div>
                    <button class="hd-picker-close" t-on-click="props.onClose">
                        <i class="fa fa-times"/>
                    </button>
                </div>

                <div class="hd-picker-search-wrap">
                    <i class="fa fa-search hd-picker-search-icon"/>
                    <input
                        type="text"
                        class="hd-picker-search"
                        placeholder="Search widgets…"
                        t-model="state.search"/>
                </div>

                <div class="hd-picker-body">
                    <t t-if="!groups.length">
                        <div class="hd-picker-empty">No widgets match your search.</div>
                    </t>
                    <t t-foreach="groups" t-as="group" t-key="group.name">
                        <div class="hd-picker-group">
                            <div class="hd-picker-group-title" t-esc="group.name"/>
                            <div class="hd-picker-cards">
                                <t t-foreach="group.items" t-as="w" t-key="w.id">
                                    <div t-attf-class="hd-picker-card {{ w.active ? 'hd-picker-card--active' : '' }} {{ !w.installed ? 'hd-picker-card--disabled' : '' }}">
                                        <div t-attf-class="hd-picker-card-icon hd-picker-icon-#{w.color}">
                                            <i t-attf-class="fa #{w.icon}"/>
                                        </div>
                                        <div class="hd-picker-card-body">
                                            <div class="hd-picker-card-label" t-esc="w.label"/>
                                            <div class="hd-picker-card-desc" t-esc="w.description"/>
                                            <t t-if="!w.installed">
                                                <span class="hd-picker-module-tag">
                                                    Module not installed
                                                </span>
                                            </t>
                                        </div>
                                        <div class="hd-picker-card-action">
                                            <t t-if="!w.installed">
                                                <span class="hd-picker-unavailable">—</span>
                                            </t>
                                            <t t-elif="w.active">
                                                <button class="hd-picker-btn hd-picker-btn--remove"
                                                        t-on-click="() => props.onRemove(w.id)">
                                                    <i class="fa fa-minus"/> Remove
                                                </button>
                                            </t>
                                            <t t-else="">
                                                <button class="hd-picker-btn hd-picker-btn--add"
                                                        t-on-click="() => props.onAdd(w.id)">
                                                    <i class="fa fa-plus"/> Add
                                                </button>
                                            </t>
                                        </div>
                                    </div>
                                </t>
                            </div>
                        </div>
                    </t>
                </div>

                <div class="hd-picker-footer">
                    <span>Drag widgets on the dashboard to reorder them. Click the resize arrows to change width.</span>
                    <button class="hd-picker-btn hd-picker-btn--defaults" t-on-click="props.onRestoreDefaults">
                        <i class="fa fa-undo"/> Restore Default Widgets
                    </button>
                </div>
            </div>
        </div>
    </t>
</t>

<!-- ── Config Panel ───────────────────────────────────────────────────────── -->
<t t-name="aura_backend_theme.DashboardConfigPanel">
    <t t-if="props.open">
        <div class="hd-config-overlay" t-on-click.self="props.onClose">
            <div class="hd-config-panel">
                <div class="hd-config-header">
                    <div class="hd-config-head-left">
                        <div class="hd-config-head-icon"><i class="fa fa-th-large"/></div>
                        <div>
                            <span class="hd-config-title">Dashboard Settings</span>
                            <div class="hd-config-subtitle">Customize visibility, cards, limits, and layout.</div>
                        </div>
                    </div>
                    <button class="hd-config-close" t-on-click="props.onClose"><i class="fa fa-times"/></button>
                </div>
                <div class="hd-config-body">
                    <div class="hd-config-section-title">Stats Visibility</div>
                    <div class="hd-config-visibility-grid">
                        <label class="hd-toggle hd-toggle-card">
                            <span>Overview Stats</span>
                            <input type="checkbox" t-att-checked="props.config.show_stats_row"
                                   t-on-change="(e) => this.onToggle('show_stats_row', e)"/>
                        </label>
                        <label class="hd-toggle hd-toggle-card">
                            <span>Mes activités</span>
                            <input type="checkbox" t-att-checked="props.config.show_activities"
                                   t-on-change="(e) => this.onToggle('show_activities', e)"/>
                        </label>
                        <label class="hd-toggle hd-toggle-card">
                            <span>Top Invoices</span>
                            <input type="checkbox" t-att-checked="props.config.show_invoices"
                                   t-on-change="(e) => this.onToggle('show_invoices', e)"/>
                        </label>
                        <label class="hd-toggle hd-toggle-card">
                            <span>My Pipeline</span>
                            <input type="checkbox" t-att-checked="props.config.show_pipeline"
                                   t-on-change="(e) => this.onToggle('show_pipeline', e)"/>
                        </label>
                        <label class="hd-toggle hd-toggle-card hd-toggle-card--full">
                            <span>Ouverts récemment</span>
                            <input type="checkbox" t-att-checked="props.config.show_recent"
                                   t-on-change="(e) => this.onToggle('show_recent', e)"/>
                        </label>
                    </div>

                    <div class="hd-config-section-title">Overview Cards</div>
                    <div class="hd-config-note">Pick four cards to show in the summary row. Available cards are enabled by installed modules.</div>
                    <div class="hd-config-cards-grid">
                        <t t-foreach="moduleSlots" t-as="slot" t-key="slot.index">
                            <div class="hd-config-row hd-config-row--stat">
                                <label>Card <t t-esc="slot.index + 1"/></label>
                                <select class="hd-config-select"
                                        t-on-change="(e) => this.onModuleSlotChange(slot.index, e)">
                                    <t t-foreach="slot.options" t-as="option" t-key="slot.index + '_' + option.key">
                                        <option t-att-value="option.key"
                                                t-att-selected="option.key === slot.value"
                                                t-att-disabled="!option.available &amp;&amp; option.key !== slot.value"
                                                t-esc="option.labelText"/>
                                    </t>
                                </select>
                            </div>
                        </t>
                    </div>

                    <div class="hd-config-section-title">Limits</div>
                    <div class="hd-config-limits-grid">
                        <div class="hd-config-stepper-wrap">
                            <label>Max Activities</label>
                            <div class="hd-config-stepper">
                                <button t-on-click="() => this.onStep('activity_limit', -1, 1, 20)">−</button>
                                <input type="number" min="1" max="20" t-att-value="props.config.activity_limit"
                                       t-on-input="(e) => this.onNumber('activity_limit', e)"/>
                                <button t-on-click="() => this.onStep('activity_limit', 1, 1, 20)">+</button>
                            </div>
                        </div>
                        <div class="hd-config-stepper-wrap">
                            <label>Max Invoices</label>
                            <div class="hd-config-stepper">
                                <button t-on-click="() => this.onStep('invoice_limit', -1, 1, 20)">−</button>
                                <input type="number" min="1" max="20" t-att-value="props.config.invoice_limit"
                                       t-on-input="(e) => this.onNumber('invoice_limit', e)"/>
                                <button t-on-click="() => this.onStep('invoice_limit', 1, 1, 20)">+</button>
                            </div>
                        </div>
                        <div class="hd-config-stepper-wrap">
                            <label>Max Pipeline Stages</label>
                            <div class="hd-config-stepper">
                                <button t-on-click="() => this.onStep('pipeline_stages_limit', -1, 2, 8)">−</button>
                                <input type="number" min="2" max="8" t-att-value="props.config.pipeline_stages_limit"
                                       t-on-input="(e) => this.onNumber('pipeline_stages_limit', e)"/>
                                <button t-on-click="() => this.onStep('pipeline_stages_limit', 1, 2, 8)">+</button>
                            </div>
                        </div>
                        <div class="hd-config-stepper-wrap">
                            <label>Max Recent Records</label>
                            <div class="hd-config-stepper">
                                <button t-on-click="() => this.onStep('recent_limit', -1, 3, 15)">−</button>
                                <input type="number" min="3" max="15" t-att-value="props.config.recent_limit"
                                       t-on-input="(e) => this.onNumber('recent_limit', e)"/>
                                <button t-on-click="() => this.onStep('recent_limit', 1, 3, 15)">+</button>
                            </div>
                        </div>
                    </div>

                    <div class="hd-config-section-title">Invoice Filter</div>
                    <div class="hd-config-row hd-config-row--single">
                        <label>Default Filter</label>
                        <select t-att-value="props.config.invoice_filter"
                                t-on-change="(e) => this.onSelect('invoice_filter', e)">
                            <option value="all">Tous</option>
                            <option value="overdue">En retard</option>
                            <option value="this_month">Ce mois-ci</option>
                        </select>
                    </div>

                    <div class="hd-config-section-title">Layout</div>
                    <div class="hd-config-density-grid">
                        <label class="hd-density-card">
                            <input type="radio" name="layout_density" value="comfortable"
                                   t-att-checked="props.config.layout_density === 'comfortable'"
                                   t-on-change="() => this.onRadio('layout_density', 'comfortable')"/>
                            <span>Comfortable</span>
                        </label>
                        <label class="hd-density-card">
                            <input type="radio" name="layout_density" value="compact"
                                   t-att-checked="props.config.layout_density === 'compact'"
                                   t-on-change="() => this.onRadio('layout_density', 'compact')"/>
                            <span>Compact</span>
                        </label>
                        <label class="hd-density-card">
                            <input type="radio" name="layout_density" value="spacious"
                                   t-att-checked="props.config.layout_density === 'spacious'"
                                   t-on-change="() => this.onRadio('layout_density', 'spacious')"/>
                            <span>Spacious</span>
                        </label>
                    </div>

                    <div class="hd-config-theme-hint">
                        Theme is fixed to Aura. Appearance mode follows Odoo setting automatically in Enterprise dark mode.
                    </div>
                </div>
                <div class="hd-config-footer">
                    <button class="hd-config-btn hd-config-btn--ghost" t-on-click="props.onClose">Cancel</button>
                    <button class="hd-config-btn hd-config-btn--primary" t-on-click="props.onClose">Save Settings</button>
                </div>
            </div>
        </div>
    </t>
</t>

<!-- ── Unavailable Widget Placeholder ────────────────────────────────────── -->
<t t-name="aura_backend_theme.WidgetUnavailable">
    <div class="hd-card hd-widget-unavailable">
        <div class="hd-card-header">
            <span class="hd-card-title"><i class="fa fa-ban"/> <t t-esc="props.widget.label or 'Unavailable Widget'"/></span>
            <button class="hd-card-action-btn" t-on-click="props.onRemove">
                <i class="fa fa-times"/> Remove
            </button>
        </div>
        <div class="hd-empty-cta hd-widget-unavailable-body">
            <i class="fa fa-plug hd-empty-icon"/>
            <p>This widget is currently unavailable because its module is not installed.</p>
            <p class="hd-widget-unavailable-note">Remove it or install the missing module to restore the card.</p>
        </div>
    </div>
</t>

<!-- ── Widget Settings Modal ─────────────────────────────────────────────── -->
<t t-name="aura_backend_theme.WidgetSettingsModal">
    <t t-if="props.open and props.widget">
        <div class="hd-widget-settings-overlay" t-on-click.self="props.onClose">
            <div class="hd-widget-settings-modal">
                <div class="hd-widget-settings-header">
                    <div class="hd-widget-settings-title">
                        <i class="fa fa-cog"/>
                        <span t-esc="props.widget.label or 'Widget Settings'"/>
                    </div>
                    <button class="hd-widget-settings-close" t-on-click="props.onClose">
                        <i class="fa fa-times"/>
                    </button>
                </div>

                <div class="hd-widget-settings-body">
                    <p class="hd-widget-settings-help">
                        Store widget-specific options as JSON. These values are saved per user and can be consumed by widget code later.
                    </p>
                    <label class="hd-widget-settings-label" for="widget-config-json">Configuration</label>
                    <textarea
                        id="widget-config-json"
                        class="hd-widget-settings-textarea"
                        rows="12"
                        t-on-input="(e) => props.onDraftChange(e.target.value)"><t t-esc="props.draft"/></textarea>
                    <t t-if="props.error">
                        <div class="hd-widget-settings-error" t-esc="props.error"/>
                    </t>
                </div>

                <div class="hd-widget-settings-footer">
                    <button class="hd-widget-settings-btn hd-widget-settings-btn--secondary" t-on-click="props.onClose">
                        Cancel
                    </button>
                    <button class="hd-widget-settings-btn hd-widget-settings-btn--primary" t-on-click="props.onSave">
                        Save settings
                    </button>
                </div>
            </div>
        </div>
    </t>
</t>

<!-- ── Custom Model Widget ───────────────────────────────────────────────── -->
<t t-name="aura_backend_theme.CustomModelWidget">
    <div class="hd-card hd-custom-widget">
        <div class="hd-card-header">
            <span class="hd-card-title">
                <i t-attf-class="fa #{props.data.icon or 'fa-table'}"/>
                <t t-esc="props.data.title or 'Custom Widget'"/>
            </span>
            <button t-if="props.data.viewAction" class="hd-card-link hd-link-btn" t-on-click="() => this.navigate(props.data.viewAction)">Ouvrir →</button>
        </div>
        <div class="hd-card-body">
            <t t-if="props.data.error">
                <div class="hd-empty-cta">
                    <i class="fa fa-triangle-exclamation hd-empty-icon"/>
                    <p t-esc="props.data.error"/>
                </div>
            </t>
            <t t-elif="props.data.widget_type === 'kpi'">
                <div class="hd-custom-kpi">
                    <div class="hd-custom-kpi-value" t-esc="formatValue(props.data.value)"/>
                    <div class="hd-custom-kpi-label" t-esc="props.data.label"/>
                    <div class="hd-custom-kpi-meta">
                        <t t-esc="formatValue(props.data.count)"/> matching records
                    </div>
                </div>
            </t>
            <t t-else="">
                <t t-if="!props.data.rows or !props.data.rows.length">
                    <div class="hd-empty-cta">
                        <i class="fa fa-table hd-empty-icon"/>
                        <p>No matching records.</p>
                    </div>
                </t>
                <t t-else="">
                    <div class="hd-custom-table-wrap">
                        <table class="hd-custom-table">
                            <thead>
                                <tr>
                                    <t t-foreach="props.data.columns" t-as="col" t-key="col.name">
                                        <th t-esc="col.label"/>
                                    </t>
                                </tr>
                            </thead>
                            <tbody>
                                <t t-foreach="props.data.rows" t-as="row" t-key="row.id">
                                    <tr t-on-click="() => this.navigate(row.action)">
                                        <t t-foreach="row.values" t-as="value" t-key="value_index">
                                            <td t-esc="formatValue(value)"/>
                                        </t>
                                    </tr>
                                </t>
                            </tbody>
                        </table>
                    </div>
                </t>
            </t>
        </div>
    </div>
</t>

<!-- ── Custom Widget Builder ──────────────────────────────────────────────── -->
<t t-name="aura_backend_theme.CustomWidgetBuilder">
    <t t-if="props.open">
        <div class="hd-widget-settings-backdrop" t-on-click="props.onClose"/>
        <div class="hd-custom-builder-modal">
            <div class="hd-widget-settings-head">
                <div>
                    <div class="hd-widget-settings-title">Create Custom Widget</div>
                    <div class="hd-widget-settings-subtitle">Pick a model and choose how the widget should display it.</div>
                </div>
                <button class="hd-widget-settings-close" t-on-click="props.onClose">
                    <i class="fa fa-times"/>
                </button>
            </div>

            <div class="hd-custom-builder-body">
                <label class="hd-custom-field">
                    <span>Widget title</span>
                    <input type="text" t-att-value="props.draft.title" t-on-input="(ev) => this.update('title', ev.target.value)" placeholder="Example: Open Sales Orders"/>
                </label>

                <label class="hd-custom-field">
                    <span>Model</span>
                    <select t-att-value="props.draft.model" t-on-change="(ev) => this.onModelChange(ev)">
                        <option value="">Choose model...</option>
                        <t t-foreach="props.models" t-as="model" t-key="model.model">
                            <option t-att-value="model.model" t-esc="model.name + ' (' + model.model + ')'"/>
                        </t>
                    </select>
                </label>

                <label class="hd-custom-field">
                    <span>Widget type</span>
                    <select t-att-value="props.draft.widget_type" t-on-change="(ev) => this.onWidgetTypeChange(ev)">
                        <option value="table">Table</option>
                        <option value="kpi">KPI</option>
                    </select>
                </label>

                <label class="hd-custom-field">
                    <span>Limit</span>
                    <input type="number" min="1" max="25" t-att-value="props.draft.limit" t-on-input="(ev) => this.onLimitChange(ev)"/>
                </label>

                <t t-if="props.draft.widget_type === 'kpi'">
                    <label class="hd-custom-field">
                        <span>Aggregation</span>
                        <select t-att-value="props.draft.aggregation" t-on-change="(ev) => this.onAggregationChange(ev)">
                            <option value="count">Count records</option>
                            <option value="sum">Sum field</option>
                            <option value="avg">Average field</option>
                        </select>
                    </label>
                    <label t-if="props.draft.aggregation !== 'count'" class="hd-custom-field">
                        <span>Measure field</span>
                        <select t-att-value="props.draft.measure_field" t-on-change="(ev) => this.onMeasureChange(ev)">
                            <option value="">Choose numeric field...</option>
                            <t t-foreach="numericFields" t-as="field" t-key="field.name">
                                <option t-att-value="field.name" t-esc="field.label + ' (' + field.name + ')'"/>
                            </t>
                        </select>
                    </label>
                </t>

                <t t-if="props.draft.widget_type === 'table'">
                    <div class="hd-custom-field hd-custom-field--wide">
                        <span>Table fields</span>
                        <div class="hd-custom-field-grid">
                            <t t-foreach="props.fields" t-as="field" t-key="field.name">
                                <label class="hd-custom-check">
                                    <input type="checkbox" t-att-checked="fieldChecked(field.name)" t-on-change="(ev) => this.onFieldToggle(field.name, ev)"/>
                                    <span t-esc="field.label"/>
                                </label>
                            </t>
                        </div>
                        <small>Choose up to 6 fields.</small>
                    </div>
                </t>

                <t t-if="props.loading">
                    <div class="hd-custom-builder-note">Loading...</div>
                </t>
                <t t-if="props.error">
                    <div class="hd-widget-settings-error" t-esc="props.error"/>
                </t>
            </div>

            <div class="hd-widget-settings-actions">
                <button class="hd-widget-settings-secondary" t-on-click="props.onClose">Cancel</button>
                <button class="hd-widget-settings-primary" t-on-click="props.onSave" t-att-disabled="props.loading">
                    Create Widget
                </button>
            </div>
        </div>
    </t>
</t>

</templates>
